{
  "name": "citas-clinica-fullstack",
  "version": "1.0.0",
  "description": "Sistema Full-Stack de Reservas de Hospitales o Clínicas (Angular 18 + Node.js Express TS)",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:all": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run install:all && npm run build:frontend && npm run build:backend",
    "start": "node backend/dist/server.js"
  }
}
